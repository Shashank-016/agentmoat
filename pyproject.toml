[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentmoat"
version = "0.1.1"
description = "Enforcement and audit layer for AI agents — argument-level tool firewall, trust scoring, tamper-evident audit log, and prompt-injection detection across the Anthropic & OpenAI SDKs, LangGraph, and MCP."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Shashank Nambiar" }]
keywords = [
    "ai",
    "agents",
    "agentic",
    "security",
    "ai-security",
    "llm-security",
    "guardrails",
    "prompt-injection",
    "firewall",
    "observability",
    "langgraph",
    "anthropic",
    "openai",
    "mcp",
    "llm",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Monitoring",
    "Typing :: Typed",
]

dependencies = [
    "anthropic>=0.40.0,<1.0",
    "pydantic>=2.7.0,<3.0",
    "PyYAML>=6.0.1,<7.0",
    "sqlalchemy>=2.0.35,<3.0",
    "aiosqlite>=0.20.0,<1.0",
    "fastapi>=0.115.0,<1.0",
    "uvicorn[standard]>=0.32.0,<1.0",
    "click>=8.1.0,<9.0",
    "httpx>=0.27.0,<1.0",
    "anyio>=4.0.0,<5.0",
]

[project.optional-dependencies]
langgraph = [
    "langgraph>=0.2.0,<1.0",
    "langchain-core>=0.3.0,<1.0",
    "langchain-anthropic>=0.2.0,<1.0",
]
embeddings = [
    "sentence-transformers>=3.2.0,<4.0",
]
openai = [
    "openai>=1.0.0,<2.0",
]
benchmarks = [
    "datasets>=2.14,<4.0",       # loads deepset/prompt-injections
    "agentdojo>=0.1.0,<1.0",     # tool-calling agent tasks (benign + indirect injection)
    "langdetect>=1.0.9,<2.0",    # per-language breakdown of injection-text results
    # For `python benchmarks/run.py --embeddings` also install the [embeddings]
    # extra (sentence-transformers) — e.g. pip install "agentmoat[benchmarks,embeddings]".
]
dev = [
    "pytest>=8.3.0,<9.0",
    "pytest-asyncio>=0.24.0,<1.0",
    "pytest-cov>=5.0.0,<6.0",
    "ruff>=0.7.0",
    "mypy>=1.11.0",
    "build",
]
all = [
    "agentmoat[langgraph,embeddings,openai,dev]",
]

[project.scripts]
agentmoat = "agentmoat.cli:cli"

[project.urls]
Homepage = "https://github.com/Shashank-016/agentmoat"
Repository = "https://github.com/Shashank-016/agentmoat"
Documentation = "https://github.com/Shashank-016/agentmoat#readme"
Issues = "https://github.com/Shashank-016/agentmoat/issues"
Changelog = "https://github.com/Shashank-016/agentmoat/blob/master/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["agentmoat", "api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.10"
strict = false
ignore_missing_imports = true
